{
  "name": "dropling-bootstrap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "preinstall": "for f in .deploy-xz/site.xz.b64.*; do base64 -d \"$f\"; done > /tmp/dropling-site.tar.xz && tar -xJf /tmp/dropling-site.tar.xz -C . && node .overrides/apply.js && node .overrides/post.js",
    "start": "node startup.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mercadopago": "^3.4.0",
    "pg": "^8.13.3"
  }
}
